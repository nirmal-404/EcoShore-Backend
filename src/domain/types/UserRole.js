const UserRole = {
  ADMIN: 'admin',
  COLLECTOR: 'collector',
  ORGANIZER: 'organizer',
  VOLUNTEER: 'volunteer',
};

const UserRoles = Object.values(UserRole);

module.exports = {
  UserRole,
  UserRoles,
};
